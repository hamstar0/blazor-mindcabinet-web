@Brainstorm features
-	Change input to a 2d map if tag 'Map (Post Type)' is given
-	Push notification prompts for additional possible information for important tags
-	Implement post markdown
-	Add icons and descriptions for tags (optional)



v2
-	Posts "permissions" (AKA user groups)
-	@Brainstorm value data for posts (contextualized by tags)
v1
-	Deploy
-	Implement admin features
-	Implement invalid session ip handling
-	Implement email validation on registration
-	Add manual-unlocked hard limit to API upload content per user (1 MB?)
-	Ensure (again) user is validated before using write or sensitive read API calls (including user API itself)
-	Implement posts per page adjuster
v0.9
-	Apply mobile device layout accommodations
-	Have any posts tagged "Push Notify" create random notifications (frequency based on priority)
-	Implement drag and drop to re-order context tags (implicitly adjusts their priority)
-	(Decide) Add a button to shuffle main view of posts
-	Add bulk tag editor for posts in view
v0.8
-	Implement post grid view
-	Styling!
-	Add landing page when not logged in
-	Add login success message alert popup
-	Show user name next to Logout button
-	Set modal dialogs to have default input focused onto when open
-	Keep post create component fixed to the bottom
-	Implement tabs of posts views
	-	Moving (drag and drop)
	-	Tab closing (button + middle click)
v0.7
-	Add tag picker (favorites, recents) for posts in post view (attached popup on post focus)
-	Implement tag removals for posts
-	Implement terms editor (included favorites editor, history view)
-	Add main view editor tabs
	-	Tabs
		-	Terms editor

		
		-	Posts views
	-	Implement tabs functionality
-	Fix data sources for each browser
-	Replace tabs with new tabs concept from scratch
-	Implement tabs of posts views (basic)
	-	@Test
	-	Set context picker to deploy new tab
	-	Define posts browser tabs component
	-	Implement multiplexed posts data sources
	-	Define tabs component
v0.6
-	@Test features
-	Detect changes to context name in context editor
-	Debug failure to display list of posts
-	Show tags' contexts in tag lists
-	Clear create post fields upon submit (in particular tags entry and list)
-	Clear tags input for post create after tag submit
-	Validate ServerDataAccess_UserAppData.Update_Async term id
-	Prevent API actions upon posts of other users
-	Change PostsContextOwners into simply PostContext.Owner
-	@B&D per-user posts
	-	Per-posts permissions - Probably.
	-	Per-tag permissions - No. Tags are universal.
	-	Context tag permissions - Complicates tags, complicates contexts. Probably no.
	-	One post, many owners - Less flexible than permissions. No abstraction of groups.
-	PostsContextOwners
	-	Populate current user's PostsContextObject list from participating PostsContextOwners
	-	Remove user tags from PostContextObject tags
	-	Ensure valid PostsContextObject API for current session choice
	-	On PostsContextObject creation, declare PostsContextOwners instance as well
	-	Remove user tags from post creations
	-	Create PostsContextOwners table
-	@B&D better way to handle PostsContext owners
	-	Add 'IsLocked' flag in PostsContextTermEntry
	-	Add user id field in PostsContextTermEntry
	-	-> Separate 'Owners' table for PostsContexts
-	Fix context inputs (again)
	-	Context tag list
		-	Reposition tag's checkbox and removal icon on same line (overlaps text)
		-	Show context with tag
		-	Set priority slider+input to be same line
	-	Set context tag input above tag list
-	Upgraded to component-based tooltips
-	Revert back to REST API (sigh)
-	@Ensure singleton services are thread safe
-	Finish caching for non-id data (mapped to id data)
-	Sanitize ClientDataAccess_PrioritizedPosts.GetByCriteria_Async, GetCountByCriteria_Async on server
-	Implement client caching
-	Implement server caching
-	Channel API calls through Hub (test)
-	Ensure API calls of any 'current user' function validate user and/or session id (if relevant)
-	Prevent invalid character inputs in term editor, context editor
-	Implement "current context" editor + picker (sidebar)
	-	On picking or editing a context, update the main posts view of that context
		-	@Ensure
			-	Post submit
			-	Store user's term id with user info
			-	Context picker
			-	Context creation
			-	Set term search choice to bypass context prompt and simply confirm the term input
			-	Show term's context in term display components
			-	Posts sorting works
		-	@Ensure posts filter tags works
		-	@Implement table builder
		-	Finish implementing SQL for querying by priority (sort by priority order)
		-	Add user-as-tag to their posts' tags
		-	@Ensure functionality (install, post create, user register, user login, context tags, contexts)
			-	Disable tag entry if no user
			-	Disable post create if no user
			-	Test registration form
			-	Test context picker
			-	Prevent password entry via enter key when invalid
			-	Fix login submit button
			-	Fix login fail error (don't indicate if user name doesn't exist)
			-	Context picker selection (defaults to current)
			-	Set each user to have a new post context as their session default
			-	Context "save changes" button state
			-	New tag posting
			-	Ensure tag widgets work
			-	Add data API validators
			-	User's current context
			-	Set TermObject to use a DatabaseEntry properties for Alias and Context
			-	Change DatabaseEntry to Raw
			-	Restructure data Objects to supply relevant FK ids as properties
			-	Logout
			-	User Register
			-	User Login
			-	Install
		-	Set context tag priority values to exist as a database table (not just session data)
		-	@Add a default tag to sample posts and to a default context
		-	@Ensure app runs
		-	@Plan
			-	Posts are queried as ANY of the context's view (except 'is required' tags)
			-	Posts are sorted by sum of their context tags' priority metrics
	-	Implement name input for current context
	-	Implement picker list of contexts
	-	Set tag set editor to initialize and apply changes with its tags to the current context
	-	Implement tag input for 'is required'
 	-	Add a "priority value" slider for tags
		-	Implement priority value storage for tags
			-	Implement session crap
				-	Load 'current context' for ui from client session data
				-	Load all contexts after user session is loaded (calls server for contexts)
			-	Implement capacity limit for history
			-	Declare table and protocols for contexts
			-	Declare table and protocols for history
			-	Declare table and protocols for favorites
		-	Add slider to each context editor tags
		-	Create component for hybrid slider + numeric input for number values
 	-	Define input for tags
 	-	Define sidebar
v0.5
-	Rename Post to SimplePost
 	-	Rename db table
 	-	Rename routes
 	-	Rename data structure
-	Alter tag picker to add popular and favorited tags
 	-	Implement tag picker button bars
 		-	Add tag quick-pick buttons for recent tags
 		-	Add tag quick-pick buttons for favorite tags
 		-	Define space for button bars
 	-	Store history the last 100 tags used
-	Implementing "favoriting" tags
 	-	Implement tag favorites table (user id, tag id)
 	-	Add favorite button in tag display component
 	-	Store favorites with session data
v0.4
-	Implement term context input
 	-	Complete new term submit after context chosen
 	-	Implement term picker
 	-	Define modal dialog for picker
 	-	Activate modal dialog for picker
-	@Test usability
-	@Test
-	Use DbString.IsAnsi
-	Implement routes via constant strings
-	Changing from SQL Server to MariaDB
-	Refactor Entry classes into Object classes
 	-	Use prototypes for db entry
 	-	Create "prototype" classes for unfinished Objects
 	-	Rename
v0.3
-	Implement users
 	-	Implement session ip validation
 	-	Ensure proper scoped session caching
 	-	Implement session expiration
 	-	Implement session resume
 	-	Define UI login prompt
 	-	Define UI registration prompt (instantiate a salt on first contact)
 	-	Define session ID + session storage
 	-	Define tables
v0.2
-	@Test
-	Create local deployment
-	Create queries for data operations
-	Create tables for data structures
-	Create special path for installing tables
-	Implement permanent database
v0.1
-	Create repo
-	Set page to 0 after search
-	@Test post filtering
-	Ensure inputting an existing term in TagSetEditor instead selects the existing term
-	Prevent inputting repeat terms (in the database, TagSetEditor, and TermEditor)
-	Implement basic styling
 	-	Organize layout
 	-	Tag editor + search dropdown
-	@Test post search
-	Implement pagination linking
-	Move posts search bar next to sort bar
-	Remove grid layout of page body
-	Fix weird pagination display
-	Fix empty term submit
-	Add title for post create component
-	Add tags display for posts
-	@Test post editor
-	@Test tags editor
-	Fix tags (plural) editor
-	Update tags picker
-	@Test tag editor
-	Implement post form
-	Add main (posts) view
 	-	At bottom, implement filter criteria based on tags
 	-	At bottom, implement search criteria based on words in text
 	-	At top, implement sort criteria toggles on a stationary button bar
 	-	Implement route component (stores page, search, sort, and filter criteria)
 		-	Implement data handling
 		-	Define basic component
 	-	Render a paginated view of posts
-	Define data structure for content: Posts (body, tags)
-	Define project + temporary database
